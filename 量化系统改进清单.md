# FisherQuant 量化系统改进清单（量化从业人员视角）

> 评审日期：2026-07-26
> 评审范围：master 分支（`fisher/` 全部核心模块）
> 评审方法：逐模块通读回测引擎、撮合、持仓、OMS、风险、组合、绩效、数据层源码
> 结论：当前系统**架构完整、分层合理**，但回测正确性存在多处会导致结果系统性失真的缺陷，结论"好看但不可信"。优先级按 P0（正确性）→ P1（策略可信度）→ P2（工程落地）排列。

---

## P0 — 回测正确性（所有绩效数字的地基，最紧急）

| # | 问题 | 证据（文件:行） | 影响 | 整改建议 |
|---|------|----------------|------|----------|
| P0-1 | **NAV 重复计算持仓市值，净值虚增约 1 倍** | `backtest/engine.py:79-85` + `paper/engine.py:110-112` | `paper.get_account()["capital"]` 已含 paper 引擎自身持仓市值，backtest 又叠加 PositionService 市值；两套账本（PaperEngine._positions 与传入的 PositionService）持有相同头寸 → 同一份市值加两遍。所有收益率/夏普/回撤失真。 | 只保留一本账：NAV = available + 单一持仓账本市值；回测与 paper 共用同一个 PositionService，删除副本。 |
| P0-2 | **同根 K 线"看到收盘价→同根收盘价成交"的向前看偏差** | `backtest/engine.py:34-43`、`paper/fill.py:31-34` | 默认 `current_close` 用已知结果价零延迟成交；`next_open` 实现错误——返回的是**当前 bar 的 open**而非下一根 open，本应最贴近实盘的模式逻辑错乱。系统性高估收益。 | 信号在 bar N 生成 → 成交价用 bar N+1 的 open（或带延迟的 close）；`next_open` 改为取下一根 bar 的 open。 |
| P0-3 | **完全没有滑点 / 市场冲击模型** | `paper/fill.py` 全文件 | 成交价仅取 close/open/vwap/ohlc4，零滑点、零买卖价差。A 股薄利策略回测与实盘差出数量级。 | 增加滑点（固定 bp + 成交量冲击，如按参与率推导的临时/永久冲击）与买卖价差成本。 |
| P0-4 | **A 股 T+1 在回测中未生效** | `position/service.py:89`（`settle_t1` 定义）、回测主循环无调用（grep 确认） | 回测直接走 `update_on_fill` 减持仓，可当日买卖刚买入的票，虚增流动性与周转收益。 | 回测每根 bar 结束后调用 `settle_t1()`；卖出前校验 `available`（已含 T+1 冻结）而非仅减总持仓。 |
| P0-5 | **风险引擎未接入回测主循环** | `backtest/engine.py` 无 `RiskEngine` 实例化/调用 | `configs/risk.yaml` 的 MaxPosition / DailyLossLimit / SectorLimit 在回测中全是死代码。 | 在 `_process_signals` 前对每笔订单调用 `RiskEngine.check()`，不通过则拒单。 |
| P0-6 | **止损/止盈条件单在回测中从不评估** | `oms/engine.py:74`（`check_conditions` 定义）、回测循环无调用 | 风控退出逻辑无法被回测验证，回测出的"最大回撤"不含止损。 | 回测每根 bar 调用 `oms.check_conditions(ticker, bar.close)` 触发挂单。 |
| P0-7 | **复权口径错误，破坏 point-in-time 正确性** | `market/akshare.py:50`（`adjust="qfq"`）；`store/schema.py` 有 `adj_factor` 列但回测未读（`time_player.py` 不加载该列） | 前复权基准日随"今天"漂移，等于用未来信息调历史价；分红/拆股、幸存者偏差污染结果。 | 数据层存**原始价 + 时点复权因子**（hfq 或自管 adj_factor）；回测按"当时点"的因子复权。 |

---

## P1 — 策略与组合框架（影响策略可信度）

| # | 问题 | 证据（文件:行） | 影响 | 整改建议 |
|---|------|----------------|------|----------|
| P1-8 | **pair_trade 回测会直接崩溃** | `strategy/builtin/pair_trade.py:60-65`（发做空腿）+ `position/service.py:41-44`（卖空 raise） | 经典统计套利策略无法回测。 | 补齐做空账户/融券模型，或回测切换为"多空净值差"记账，避免负持仓抛异常。 |
| P1-9 | **无任何过拟合防护** | `strategy/engine.py` 仅"加载+运行" | 无 train/test 切分、walk-forward、多重比较校正（Deflated Sharpe / 族wise error）。网格搜参必然过拟合且无提示。 | 增加样本内外切分、walk-forward、参数稳定性与 Deflated Sharpe Ratio 报告。 |
| P1-10 | **信号数量与权重脱节** | `portfolio/builder.py:91-104` | 用 `capital*weight` 算资金分配，但下单 `quantity` 仍取信号里写死的 100 股，权重没落到实际下单量。 | 下单数量由权重推导：`qty = floor(capital*weight / price)`，信号只出方向/置信度。 |
| P1-11 | **Kelly / risk_parity 名不副实** | `portfolio/methods.py:15-53` | `kelly` 把各标的独立 f* 直接归一化（忽略相关性，极端可爆仓）；`risk_parity` 实为逆波动率加权，非等风险贡献。 | Kelly 用组合层优化（考虑协方差/约束）或改名"分数化凯利"；risk_parity 实现等风险贡献迭代。 |

---

## P2 — 工程与可运营性（量化团队落地必须）

| # | 问题 | 证据（文件:行） | 影响 | 整改建议 |
|---|------|----------------|------|----------|
| P2-12 | **数据层短板** | `market/akshare.py:54`（分钟线 return []） | 因子与回测只能日频；无盘口/实时；无基准指数自动拉取。 | 接入支持分钟/实时数据的源；自动拉取基准（沪深300/中证500）用于归因。 |
| P2-13 | **实时/实盘缺位** | `broker/adapter.py`（仅 PaperEngine 实现）、`system.yaml:2`（mode: paper） | 无真实券商适配；paper 引擎账户状态无锁，monitor websocket 与 oms 实时路径有竞态。 | 实现真实 BrokerAdapter；账户状态加锁 / 用 asyncio 串行事件循环。 |
| P2-14 | **可复现性为零** | 全局无随机种子/数据快照/版本管理 | 叠加 P0-7 的 qfq 漂移，同一策略不同日期跑结果可能不同，无法审计。 | 数据快照 + 版本号 + 运行参数落库；每次回测记录输入哈希。 |
| P2-15 | **绩效指标小瑕疵** | `analytics/performance.py` | Sharpe 用总体方差（÷n 非 n−1，轻微低估波动）；alpha/IR 用 `rets_p[-n:]` 切片对齐而 beta 用全样本，口径不一致；turnover 分母用平均 NAV 非平均总持仓；无扣费前后对比。 | 统一用样本方差；对齐 beta/alpha/IR 的样本窗；turnover 用平均总持仓；增加"扣费前后收益对比"看成本拖累。 |
| P2-16 | **涨跌停未建模** | `paper/fill.py:43-53` | A 股 ±10%/±20% 限制下常无法成交，但 price_limit 只判下单限价越界，不判当日是否涨停可交易。 | 撮合时校验当日是否涨停（买涨停/卖跌停拒单或挂单不成交）。 |

---

## 建议实施顺序

**第一批（地基，必须最先做）**：P0-1、P0-2、P0-3、P0-7
> 这四项决定了所有数字是否可信。改完回测结果才可能从"好看的故事"变成"能信的数字"。

**第二批（让风控可验证）**：P0-4、P0-5、P0-6、P1-8
> 把 T+1、风险引擎、止损条件单真正接入回测，并修 pair_trade 崩溃，策略才具备可回测的完整性。

**第三批（策略质量与落地）**：P1-9、P1-10、P1-11、P2-12 ~ P2-16
> 过拟合防护、权重落地、实盘适配、可复现性、指标修正。

---

## 复检清单（交付前逐条勾选）

- [ ] NAV 仅由单一持仓账本计算，无重复记账
- [ ] 回测成交价晚于信号生成的 bar（无同根向前看）
- [ ] `next_open` 取下一根 open；已引入滑点/冲击成本
- [ ] 回测每根 bar 调用 `settle_t1()`，T+1 生效
- [ ] `RiskEngine` 在回测下单前校验，拒单可观测
- [ ] 止损/止盈条件单在回测中被 `check_conditions` 触发
- [ ] 数据按时点复权（hfq 或 raw+adj_factor），回测读取复权因子
- [ ] pair_trade 等多空策略可完整跑通
- [ ] 存在样本外/walk-forward 与过拟合度量（如 Deflated Sharpe）
- [ ] 下单数量由组合权重推导，非写死
- [ ] 绩效指标口径一致、含扣费前后对比
- [ ] 涨跌停不可成交已被建模
