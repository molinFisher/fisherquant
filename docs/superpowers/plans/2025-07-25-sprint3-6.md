# FisherQuant Sprint3-6 — Backtest + Visual + Report + Config

> **For agentic workers:** Use superpowers:subagent-driven-development.

**Goal:** Build backtest center (single/multi/walk-forward/sensitivity/history/market-regime), visualization dashboard (equity/K-line/drawdown/heatmap/histogram), report center (PDF/HTML), quote board, and system config.

**Architecture:** All Dash pages. Backtest via background callbacks (INF-01). Results persisted via BacktestSerializer (INF-04). Charts via Plotly. Reports via Jinja2 + weasyprint.

## Global Constraints
- Python >= 3.11, Dash + dbc, Plotly, diskcache
- Background callbacks for all backtest execution
- Results via INF-04 BacktestSerializer
- Charts via INF-09 LTTB downsampling (≤500 pts)
- All color specs per PRD: K-line 红涨绿跌, analysis 绿盈红亏
- 479+ existing tests continue passing

---

### Tasks

**S3-01: Backtest Center — Single Strategy**
Files: fisher/dash_app/pages/backtest_center.py, callbacks/backtest_callbacks.py
- Config form: strategy dropdown, symbol multi-select, date range, capital/commission/slippage/benchmark
- Background callback: submit → progress bar → inline summary (core metrics + equity thumbnail) → "view full dashboard" link
- Cancel support, double-click prevention

**S3-02: Backtest Center — Multi-Compare + Walk-Forward + Sensitivity**
- Multi: up to 5 strategies, parallel submit, comparison table + overlaid equity
- Walk-forward: window count (4-24), per-window performance table + aggregate stats
- Sensitivity: parameter grid (1-2 params), heatmap (dual param) or line chart (single param)

**S3-03: Backtest Center — History + Market Regime**
- History table: saved_from + sort + click to load via `?backtest_id=`
- Auto-save every backtest (INF-04), 200 record limit with cleanup
- Market regime: split by bull/bear/sideways (based on benchmark trend), comparison table, colored bands on equity curve

**S3-04: Visualization Dashboard**
- Pages: equity curve (strategy blue vs benchmark gray dashed), drawdown (red fill), monthly heatmap (绿盈红亏), daily return histogram, trade log table (paginated 20/page)
- K-line: candle chart w/ red-up green-down, buy/sell markers (triangles), symbol switcher dropdown
- URL-driven: `?backtest_id=xxx` loads from Parquet via INF-04

**S4-01: Report Center + Quote Board + System Config**
- Report: PDF/HTML generation via Jinja2 + weasyprint, section checkboxes, preview modal
- Quote board: watchlist table (code/name/price/change%/volume), add/remove, manual refresh, auto-refresh 60s during trading hours
- Config: global backtest params form (commission/slippage/capital), benchmark config, data refresh strategy (cron), data source management (AKShare only, others grayed), system log viewer (filter by level)

**S5-01: UX Polish + Integration Test**
- Backtest Tabs (4 mode tabs sharing common config), first-use guide (3-step + demo data), responsive layout completion
- E2E test: new user → search stock → fetch data → create strategy → run backtest → view results → export report

---

Implement ALL tasks sequentially. Commit after each. Run `pytest tests/ -v` after all.

Work from: C:\Users\Administrator\FisherQuant
Report: C:\Users\Administrator\FisherQuant\.superpowers\sdd\2025-07-25-sprint3-6\task-s3-report.md

Return: N commits, final test count.