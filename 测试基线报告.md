# FisherQuant 测试基线报告（Phase 0 执行结果）

> 对应《测试计划.md》中 "Phase 0 · P0 立即可执行项"。本报告记录执行后的真实基线，
> 以及本轮发现并修复的致命断点。

## 1. 测试环境

| 组件 | 版本 / 说明 |
| --- | --- |
| pytest | 9.1.1（此前未安装，整套测试无法运行） |
| pytest-asyncio | asyncio_mode=auto |
| pytest-cov | 覆盖率采集 |
| pytest-timeout | `--timeout=30 --timeout-method=thread`（暴露挂起，避免整套卡死） |
| Python | 3.13.12（项目隔离 .venv，未污染全局） |
| DuckDB | 本环境版本**不支持** `PRAGMA auto_commit`（会抛 `CatalogException`） |

仓库根 `pyproject.toml` 已声明 `testpaths=["tests"]`，`pytest` 可直接收集。

## 2. Phase 0 已完成的动作

1. **安装 pytest 依赖栈**（原先缺失，全部测试无法运行）。
2. **新建 `tests/regression/` 两个强断言回归套件**，把根目录游离的 16 项量化修复纳入
   自动收集保护：
   - `test_quant_fixes.py`（P0-1~P0-6、P1-8、P2-16，8 例）
   - `test_improvement_fixes.py`（P1-11、P2-15、P1-9、P2-14、P0-5、P2-13，10 例）
   - 两文件单独运行 **18 passed**。
3. **mock 化 `tests/unit/test_market_akshare.py`**，离线可跑（接收 `mock_akshare` fixture）。
4. **隔离 3 个失效 / 网络测试**（改名为 `_broken_*` / `network_*` 避免被 `test*` 默认收集）：
   - `test_data_download.py` / `test_auto_fixer.py`：引用不存在的 `fisher_temp` 包，收集即 `ModuleNotFoundError`。
   - `test_market_to_store.py`：收集阶段执行 `is_akshare_available()` 真实网络探测，网络不稳时卡死整套。
5. **补齐 `tests/conftest.py` 多处 mock 缺口**，消除收集 / 运行期真实触网：
   - `mock_index_cons` 新增 `stock_zh_a_daily` / `stock_hk_daily` / `stock_hk_spot`（修复 `auto_load_service` 触网）
   - `mock_akshare` 新增 `stock_hk_spot` / `stock_zh_a_hist_min_em` 与 `MockAKShareSeries.astype`
   - `MockDF` 补 `.columns` 属性与 `.head(n)` 方法（修复 `_load_index_codes` 访问 `df.columns[0]` / `hk_df.head(80)`）
6. **修复 DuckDB 读写死锁相关缺陷**（`fisher/store/engine.py` + 用例侧）：
   - `query_df` 改为每次使用**独立临时连接并立即关闭**，消除读连接持有快照阻塞写者（DuckDB 读写死锁）。
   - 修复 `connect()` 中**无效的 `PRAGMA auto_commit=true`**：本环境 DuckDB 版本不支持该 PRAGMA，
     原代码抛 `CatalogException` 后 `break`，导致**读连接池恒为空**且**泄漏**刚打开的连接。
     DuckDB 读连接默认即 autocommit，去除该 PRAGMA 后读池正常建立。
   - `test_data_service_search_no_crash` 补充 `mock_akshare`：原直连真实网络 `ak.stock_info_a_code_name()`。
   - 集成 `mock_all_akshare` 补 `stock_hk_spot` / `stock_zh_a_hist_min_em`：`search_symbols` 第三步原直连真实网络。

## 3. 全量基线结果

```
命令：pytest tests/unit tests/regression tests/integration --timeout=30 --timeout-method=thread -p no:cacheprovider --cov=fisher
结果：569 passed, 14 failed, 56% 覆盖率（fisher/*），耗时 ~39s
```

**对比（执行前 → 执行后）**

| 维度 | 执行前 | 执行后 |
| --- | --- | --- |
| pytest 是否可运行 | 否（未安装） | 是 |
| 全量能否跑完 | 否（真实网络卡死 / 收集报错） | 是（~39s） |
| 通过率 | 不可测 | 569 / 583（97.8%） |
| 代码覆盖率 | 0% / 不可测 | 56% |
| 真实网络依赖 | 有（多处直连） | 已 mock（离线可跑） |

## 4. 14 个失败 —— 均为既有缺陷（非本轮改动引入）

验证方式：14 个失败所在的测试文件（`test_paper_engine` / `test_portfolio_builder` /
`test_position_service` / `test_order_to_position` / `test_startup`）**均未被本轮修改**；
git 变更足迹仅涉及 `engine.py`(读池修复)、`conftest.py`、`test_dash_callbacks.py`、
`test_market_akshare.py`、`test_regression_fixes.py` 与新增 `tests/regression/`。
失败均为**纯逻辑断言**（`assert 0 == 1`、`TypeError: NoneType`、`did not raise`），与
DuckDB / 网络无关。

| 模块 | 失败数 | 现象 | 根因类型 |
| --- | --- | --- | --- |
| Paper Trading 仿真引擎 `on_bar` 撮合 | 4 | `fill` 返回 0，订单未撮合 | 撮合逻辑与 bar 结构不匹配（产品 bug） |
| 组合构建 / 仓位权重 `portfolio_builder` | 3 | kelly 权重 0.425（断言期望 <0.01）；等额权重返回 0 / 全额资本 | 权重计算 or 测试期望偏差（产品/测试 bug） |
| 持仓服务 `position_service` | 2 | 加权成本 11.0 vs 11.05（精度）；超卖未抛 `ValueError` | 缺超卖守卫（产品 bug）+ 精度 |
| OMS → Paper → 持仓 流水线 | 4 | 流水线产出 0 持仓 / `NoneType` 下标 | 集成链路断裂（产品 bug） |
| 全系统启动冒烟 `startup` | 1 | 全链路 `0 == 1` | 上游链路的汇总表现 |

## 5. 《测试计划》原文三大致命断点的处置

1. **pytest 未安装** → ✅ 已装（项目隔离 .venv）。
2. **16 项量化修复游离于 pytest 之外** → ✅ 已纳入 `tests/regression/`（18 例强断言，18 passed）。
3. **直连真实网络** → ✅ 已全面 mock（conftest 两 fixture + 集成 `mock_all_akshare` + `search_symbols` 用例）。
   - 附带：原**无 CI / 覆盖率门禁** → 建议 Phase 2 增加 GitHub Actions（见下）。

## 6. 下一步（Phase 1 起）

- **Phase 1（修复 14 个既有失败）**：按模块分——Paper 撮合、Portfolio 权重、Position 守卫、OMS→Position
  流水线、Startup 冒烟。需逐一定位根因（产品逻辑 or 测试期望），优先修**产品侧**真实 bug。
- **Phase 2（CI / 门禁）**：加 GitHub Actions 跑 `pytest --cov`，设覆盖率下限与 `--timeout` 防回归。
- **Phase 3+（覆盖率缺口）**：当前 56%，低覆盖模块集中在 `risk/realtime.py`(23%)、
  `strategy/dsl.py`(20%)、`strategy/execution.py`(8%)、`store/exporter.py`(0%)、
  `visualization/downample.py`(6%)，按《测试计划》覆盖率缺口矩阵补齐。

---

## 7. Phase 1 执行结果 —— 14 个失败全部修复（全绿基线）

```
命令：pytest tests/ --cov=fisher --timeout=30 --timeout-method=thread
结果：590 passed, 0 failed, 56% 覆盖率，耗时 ~38.5s
```

**通过率：569 → 590 passed，14 failed → 0 failed（100% 通过）。**

逐一定位根因后，14 个失败中 **13 个为测试期望过时（stale test）**，**1 个为产品侧真实 bug**。
坚持"优先修产品侧真实 bug，测试期望错误则同步修正"原则：

| # | 模块 | 失败数 | 根因 | 修复侧 | 处置 |
| --- | --- | --- | --- | --- | --- |
| 16 | Paper `on_bar` 撮合 | 4 | 测试期望首次 `on_bar` 即撮合，违背 **P0-2 延迟一根 bar 成交** | 测试 | 提交后连续两次 `on_bar`（先登记后撮合）；`_make_bar` 修正 high/low |
| 17 | Portfolio 权重 | 3 | Signal 未设 `limit_price`（→price=0）；kelly 断言用未截断 f*；资本累加误用 `o.price` | 测试 | 补 `limit_price`；断言稳健凯利 0.125（P1-11 截断 0.25×0.5）；改 `sum(price*qty)` |
| 18 | Position 服务 | 2 | ①`avg_cost` 未计佣金（**产品 bug**）②超卖期望 raise，违背 **P1-8 截断为平多** | **产品**+测试 | 产品：`avg_cost` 计入佣金（摊薄成本，与 A 股券商口径一致）；测试：改为断言截断至归零 + 补 allow_short 负持仓用例 |
| 19 | OMS→Paper→持仓 流水线 | 4 | 单次 `on_bar` → `filled_qty=0` → 持仓 `None` 下标崩溃（P0-2） | 测试 | 每次提交后补第二根 fill bar |
| 20 | 全系统启动冒烟 | 1 | 同 P0-2（单次 `on_bar` 未撮合） | 测试 | 补第二根 fill bar |

### 唯一产品侧修复：持仓成本基准计入佣金（`fisher/position/service.py`）

- 修复前：`avg_cost = (Σ 成交额) / 总股数`，**不含佣金**。
- 修复后：`avg_cost = (Σ 成交额 + 本笔佣金) / 总股数`，即**摊薄成本**（与 A 股券商"成本价"口径一致），
  使未实现盈亏 `= 市值 − avg_cost×qty` 反映真实经济损益。仅在同方向加仓（买入扩多/卖出扩空）时计入佣金。
- 影响验证：全仓测试中仅 `test_weighted_average_cost` 断言含佣金成本（11.05），其余用例佣金为 0，无回归。

### 关键设计澄清（供后续测试对齐，避免再次误判为 bug）

1. **P0-2 延迟一根 bar 成交**：`submit_order` 后需 **两次** `on_bar` 才成交（第一次仅把订单挪入 `_deferred`，第二次撮合）。
2. **P1-8 不做空时超卖截断为平多**：`allow_short=False` 时卖出超过持仓 **不抛异常**，净头寸归零（原 raise 会导致 pair_trade 回测崩溃）。
3. **P1-11 稳健凯利**：`f*` 截断 `max_per_asset=0.25` 再乘 `fraction=0.5`，负 edge 剔除，总和≤1 不向上归一化。

---

## 8. Phase 2 执行结果 —— 补齐单元级缺口（覆盖率 56% → 65%）

> 对应《测试计划》"Phase 2 — 补齐单元级缺口（按风险/LOC，P1）"。
> 注：本报告第 6 节曾按旧命名把"Phase 2"写成 CI/门禁；此处以《测试计划》权威定义为准——
> **Phase 2 = 单元级缺口补齐**，CI/门禁单列 Phase 6。

### 8.1 全量结果

```
命令：pytest tests/ --cov=fisher --cov-report=term-missing -q
结果：792 passed, 0 failed, 65% 覆盖率，耗时 ~72s
对比（Phase 1 后 → Phase 2 后）：590 → 792 passed（+202 例），56% → 65% 覆盖率
```

### 8.2 16 个目标模块覆盖提升（基线 → Phase 2 后）

| 模块 | 前 | 后 | 说明 |
| --- | --- | --- | --- |
| `strategy/execution.py` | 8% | **100%** | 6 个内置策略（sma_cross/macd/bollinger/rsi/buy_and_hold/custom_dsl）on_bar 信号生成全测 |
| `strategy/dsl.py` | 20% | 97% | 原语 + composite(AND/OR) + validate_dsl 校验 |
| `strategy/walk_forward.py` | 86% | **100%** | fold 边界无泄漏、比例 |
| `risk/pre_trade.py` | 97% | **100%** | 逐条规则命中/未命中边界 |
| `risk/factory.py` | 62% | **100%** | `build_risk_engine(load_risk_config())` 规则数=4 |
| `risk/realtime.py` | 23% | **100%** | 实时拦截/回撤/beta/Var（清理重复死代码后达 100%） |
| `portfolio/methods.py` | 94% | **100%** | hypothesis 属性测试：Σw≈1、ERC 风险贡献相等、kelly 有界 |
| `analytics/stats.py` | 67% | **100%** | 空/单点/全相等边界 |
| `broker/adapter.py` | 100% | **100%** | ABC 契约 |
| `broker/live.py` | 67% | **100%** | 缺凭证 `ValueError`、接口契约 |
| `market/rate_limiter.py` | 56% | **100%** | 限流窗口/计数/并发 |
| `market/ticker.py` | 79% | **100%** | ticker 解析（含幂等/非法） |
| `store/exporter.py` | 0% | **100%** | CSV/Parquet/流式导出格式与回读 |
| `config/hot_reload.py` | 0% | **100%** | 热加载触发/回调/hash 校验 |
| `visualization/downsample.py` | 6% | 98% | LTTB 降采样保形（修复除零后仅余 1 行防御分支） |
| `monitor/ws.py` | 40% | **100%** | WS 消息协议/广播/鉴权关闭码 |

### 8.3 本轮修复的产品侧真实 bug（3 处）

| # | 模块 | 问题 | 修复 |
| --- | --- | --- | --- |
| 1 | `visualization/downsample.py` | `lttb` 在 `threshold<=2` 时 `bucket_size` 除零崩溃 | 函数开头守卫：按请求点数端点裁剪（0→空、1→首点、2→首末点），显式处理空输入 |
| 2 | `analytics/performance.py` | `cumulative_return` 在 `nav[0]==0` 时 `ZeroDivisionError` | `nav[0]==0` 时返回 `0.0`（与 `len<2` 一致），避免潜在崩溃 |
| 3 | `risk/realtime.py` | `check_drawdown` 被定义两次（55-63 与 68-76），第一份为不可达死代码 | 删除重复定义，方法唯一，覆盖达 100% |

> 另有 1 处**设计层面提示**（非崩溃 bug，未改动以避免改变既有行为）：`strategy/walk_forward.py` 的
> `train_size` 参数实际未被切分逻辑使用（fold 实际用 `n//(n_splits+1)`）。当前测试不依赖该参数且均通过，
> 列为已知限制，留待后续单独决策是否让切分真正尊重 `train_size`。

### 8.4 新增测试文件（共 +202 例，~200 个为 Phase 2 新增强断言）

- `tests/unit/test_strategy_builtins.py`（~13）、`tests/unit/test_dsl.py`（~13）
- `tests/unit/test_risk_realtime.py`(22)、`test_risk_factory.py`(11)、`test_pre_trade.py`(20)、`test_portfolio_methods.py`(13)
- `tests/unit/test_exporter.py`(8)、`test_config_hot_reload.py`(12)、`test_downsample.py`(13)、`test_monitor_ws.py`(13)
- `tests/unit/test_broker.py`(14)、`test_rate_limiter.py`(10)、`test_ticker.py`(17)、`test_analytics_stats.py`(14)、`test_walk_forward.py`(7)

所有新测试均为强断言（数值/状态/格式校验），无纯 `no_crash`；akshare 全部 mock，离线可跑。
