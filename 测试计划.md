# FisherQuant 全面测试计划（Test Plan）

> 角色：测试专家评审。对象：FisherQuant（A股/港股通量化交易平台，Python，`master` 分支）。
> 目标：盘点现有测试用例、评估质量、识别缺口，制定一套可落地、可 CI 化的全面测试计划。
> 评审日期：2026-07-26。范围：`fisher/` 全部模块 + `tests/` 测试树 + 根目录测试脚本。

---

## 执行摘要（Executive Summary）

现有测试资产**数量可观但存在 3 个致命断点**，导致"有测试却不可信、不可跑、不覆盖最关键逻辑"：

1. **pytest 根本没装进运行环境**（venv 内 `import pytest` → `ModuleNotFoundError`）。`pyproject.toml` 声明了 `pytest/pytest-asyncio/pytest-cov`，但当前 `.venv` 只装了运行时依赖。**结论：现有的 583 个 pytest 用例当前一个都跑不了。**
2. **最关键的 16 项量化引擎正确性修复（P0-1~7、P1-8~11、P2-12~16）游离在 pytest 体系之外**。它们只存在于根目录两个脚本 `test_backtest_fixes.py`(8 用例)、`test_improvement_smoke.py`(10 用例) 中，而 `pyproject` 的 `testpaths=["tests"]` **不收集根目录脚本**。T+1、滑点、NAV 单一账本、消除向前看、涨跌停封板、止损条件单、做空、Kelly/风险平价、DSR、walk-forward 等**全部脱离回归保护**。
3. **存在直连真实网络的单位测试** `test_market_akshare.py`（0 处 mock/monkeypatch），在 CI 与离线环境会随机失败；且 **`pyproject` 无覆盖率配置**（pytest-cov 已声明但未设 `--cov` 目标与阈值）。

其余问题：多数单元测试为"冒烟级"弱断言（`assert isinstance(list)`、`no_crash` 类），部分高风险模块零测试（broker、strategy/execution 323 行、risk 子模块、visualization、monitor/ws 等），Dash 回调层仅 HTTP 冒烟无单元级，无 CI 流水线、无覆盖率门禁、无确定性/种子回归、无变异测试。

**建议优先级**：先把测试"跑起来并锁住关键修复"（Phase 0 + Phase 1），再补缺口（Phase 2~3），最后上规模/性能/CI 门禁（Phase 4~6）。

---

## 一、现状盘点（Current State Inventory）

### 1.1 测试资产清单

| 位置 | 文件数 | 用例数 | 是否被 pytest 收集 | 备注 |
|---|---|---|---|---|
| `tests/unit/` | 47 | ~507 | ✅ | 按模块单测，命名规范 |
| `tests/integration/` | 6 | ~43 | ✅ | 含 Dash HTTP 冒烟、数据流水线、OMS→持仓等 |
| `tests/performance/` | 1 | 6 | ✅ | 回测基准（含 `slow` 标记） |
| 根目录 `test_backtest_fixes.py` | 1 | 8 | ❌（testpaths 不含根） | **16 项量化修复回归** |
| 根目录 `test_improvement_smoke.py` | 1 | 10 | ❌ | **P1-9/11, P2-13/14/15, P0-5 冒烟** |
| 根目录 `test_auto_live.py` | 1 | 0 | ❌ | 运行脚本，无断言 |

**合计**：pytest 可收集 **583** 个 `def test_`；另 18 个关键用例游离在外。

### 1.2 基础设施

- **pytest 配置**（`pyproject.toml`）：`asyncio_mode="auto"`、`testpaths=["tests"]`、marker `slow`。
- **conftest 设施**（质量较高）：
  - `tests/conftest.py`：`in_memory_db`（DuckDB 内存库 + 建表）、`RateLimiter` 夹具、`DataCenterService`、`AutoLoadService`、完备的 **akshare mock**（MockAKShareDF/Series/BoolList），覆盖 `stock_zh_a_hist`/`index_stock_cons`/`stock_info_a_code_name` 等。
  - `tests/performance/conftest.py`：性能基准夹具。
- **无 CI**：`.github/workflows` 不存在；无 `tox.ini`；无覆盖率门禁。
- **类型/ lint**：声明 `mypy`、`ruff`（未在测试流程中强制）。

### 1.3 关键问题（阻断项）

- ⛔ **pytest 未安装** → 整套 `tests/` 当前不可执行。
- ⛔ **关键修复游离** → 16 项量化正确性修复无回归保护。
- ⚠️ **直连网络单位测试** → `test_market_akshare.py` 无 mock，CI 必挂。
- ⚠️ **无覆盖率配置** → 无法量化/门禁覆盖。

---

## 二、测试质量评估（Quality Assessment）

### 2.1 优势（Strengths）

- 夹具设计扎实：内存 DuckDB、akshare 全量 mock、单例在测试间 `reset_singletons`，隔离性好。
- 有**并发测试**：`test_regression_fixes.py::TestConnectThreadLock` 用 10 线程锤 `DuckDBManager.connect`，验证线程安全——这种"坏味道猎手"测试值得推广。
- 集成测试覆盖**完整数据流水线**（搜索→拉取→缓存→自动加载→导出→删除）并用 `app.server.test_client()` 做 Dash 页面 HTTP 冒烟。
- 性能基准已存在（6 例），`slow` marker 已就位。
- 命名与目录分层清晰（unit/integration/performance）。

### 2.2 弱点（Weaknesses）

1. **关键逻辑无回归保护**：16 项量化修复仅根目录脚本，CI 不跑。
2. **弱断言普遍**：如 `test_data_service_search_no_crash` 仅 `assert isinstance(result, list)`，不校验返回内容；大量 `status=="ok"` 式断言，未验证数值正确性。
3. **高风险模块零测试**：见第三节矩阵。
4. **Dash 回调层仅 HTTP 冒烟**：13 个回调文件（含 `backtest_callbacks.py` 1156 行、本次重接了 `trade_date/bar_time`、滑点、风险引擎、seed 的路径）无**单元级**测试；layout/pages 仅 200 状态码校验。
5. **无确定性/种子回归**：`backtest/repro.py`（种子 + 输入指纹）未被套件覆盖，无法防止"同输入不同 NAV"的静默漂移。
6. **数值模块缺属性/容差测试**：`performance.py` 的 Sharpe/PSR/DSR、`portfolio/methods.py` 的权重和=1、风险平价风险贡献相等，仅冒烟未做边界与属性校验。
7. **负面/边界场景薄**：涨跌停（仅 1 例）、T+1 冻结→解冻、部分成交、做空保证金、风险拒单路径覆盖不足。
8. **性能/规模测试不足**：`strategy/execution.py`（323 行，最重策略模块）无性能测试；无大规模（如 500 标的 × 5 年日线）回测时延/内存预算。
9. **无变异测试**：无法确认测试真的能抓 bug。
10. **网络依赖**：`test_market_akshare.py` 直连 akshare，需改造为 mock。

---

## 三、覆盖率缺口矩阵（Coverage Gap Matrix）

| 功能域 / 模块 | 现有测试 | 缺口等级 | 说明 |
|---|---|---|---|
| **回测引擎** backtest/engine | 6（弱） | 🔴 高 | 仅迭代计数；16 项修复未入套件 |
| **模拟盘** paper/engine·fill·fees | 16+16+14 | 🟢 较好 | 建议补滑点/涨跌停边界 |
| **OMS** oms/engine·orders | 23+22 | 🟢 较好 | — |
| **风险引擎** risk/engine | 22 | 🟢 较好 | 但 **pre_trade/realtime/factory 无专属测试** |
| **组合构建** portfolio/builder | 18 | 🟡 中 | methods.py(Kelly/ERC)仅冒烟，需属性测试 |
| **绩效/分析** analytics/performance | 23 | 🟡 中 | stats.py **零测试**；DSR 边界未锁 |
| **因子框架** factor/* | 7 文件 | 🟢 较好 | storage.py 缺 |
| **策略** strategy/base·builtin·registry·service | 10+23+12+17 | 🟡 中 | **dsl.py、execution.py(323)、walk_forward.py 零单测** |
| **数据服务** market/*·store/* | 多文件 | 🟡 中 | **rate_limiter/ticker 无专属**；akshare 直连网络 |
| **Dash 回调** dash_app/callbacks/* | 仅集成 15 | 🔴 高 | 13 文件无单元级，含重接路径 |
| **Dash 页面/布局** layout·pages | 仅 HTTP 冒烟 | 🟡 中 | 无组件状态断言 |
| **Broker** broker/adapter·live | **0** | 🔴 高 | 实盘适配器占位/接口无测试 |
| **可视化** visualization/downsample | **0** | 🟡 中 | 降采样逻辑无测试 |
| **配置热加载** config/hot_reload | **0** | 🟡 中 | — |
| **监控** monitor/ws | **0** | 🟡 中 | WebSocket 无测试 |
| **导出** store/exporter | **0** | 🟡 中 | — |

---

## 四、全面测试计划（Comprehensive Test Plan）

### Phase 0 — 让测试"跑起来"（阻断项，P0）

目标：可重复运行的绿色基线与覆盖率数字。

- [ ] 在 `.venv` 安装测试依赖：`.venv/Scripts/python.exe -m pip install pytest pytest-asyncio pytest-cov`（建议在 `pyproject` 增加 `[project.optional-dependencies] test = [...]` 并 `pip install -e ".[test]"`）。
- [ ] 建立基线：运行 `pytest -q` 记录通过/失败数；运行 `pytest --cov=fisher --cov-report=term-missing` 记录行/分支覆盖率。
- [ ] 修复收集：把根目录 3 个脚本纳入体系——将 `test_backtest_fixes.py`、`test_improvement_smoke.py` 改造为 `tests/regression/test_quant_fixes.py` 等真正的 pytest 用例（保留其强断言），`test_auto_live.py` 移入 `tests/manual/` 或加 `skip`。
- [ ] **改造 `test_market_akshare.py`**：全部 akshare 调用改为 monkeypatch mock，去除真实网络依赖；`test_data_download.py` 确认仅走 mock 并保留 `slow`。
- [ ] 在 `pyproject` `[tool.pytest.ini_options]` 增加 `addopts = "--cov=fisher --cov-branch --cov-report=term-missing"` 与 `fail_under` 阈值（初设 60%，逐步提升）。

**产出**：可一键 `pytest` 跑通；首份覆盖率报告。

### Phase 1 — 锁住 16 项量化正确性修复（最高价值，P0）

目标：将"系统判定最优方案"的每一项转为可回归的强断言用例，归入 `tests/regression/`。

| 改进项 | 建议回归用例（强断言） |
|---|---|
| P0-1 NAV 单一账本 | 跑策略后 `account.nav == cash + Σ(position.market_value)`，且与独立重算一致 |
| P0-2 消除同根向前看 | 信号在 bar_t 产生，**成交价必须来自 bar_{t+1}**（断言 fill 的 bar_time > signal bar_time） |
| P0-3 滑点 | 买单成交价 = 下一open×(1+bps/1e4) 且 > open；卖单反向；边界 bps=0 时等于 open |
| P0-4 T+1 | 当日买入 `available==0`；下一交易日 `available==qty` 且可卖 |
| P0-5 风险引擎接入 | 预设仓位超限 → `risk_engine.check` 拒单，`rejections>=1`，持仓不增 |
| P0-6 止损条件单 | 价格跌破止损线 → 条件单触发并成交；未触发则不成交 |
| P0-7 复权口径 | 用原始价（adjust=""）回测，断言与 qfq 结果不同且更 point-in-time |
| P1-8 做空 | 无持仓卖出 → 负持仓 `qty<0`，保证金占用正确 |
| P1-9 walk-forward | `walk_forward` 返回 N 个 OOS fold；DSR 在样本外稳定 |
| P1-10 信号数量 | 信号显式 `qty` 优先，买单受权重额约束；卖单不被权重放大（复现"500 股被放大成 10 万"的反例） |
| P1-11 Kelly/风险平价 | `sum(weights)≈1`；risk_parity 低风险标的权重更高；ERC 风险贡献两两差 < 1e-3；kelly 总和≤1 时**保留现金不向上归一化** |
| P2-12 输入指纹 | 同输入 → `compute_input_hash` 同值；任一参数变 → 不同值 |
| P2-13 实盘适配器 | `LiveBrokerAdapter()` 无凭证 → 抛 `ValueError`（非 NotImplementedError） |
| P2-14 种子可复现 | 同 `seed` 两次回测 NAV 序列完全一致；不同 seed 不同 |
| P2-15 DSR/Acklam | **负 Sharpe → DSR≈0（非 1.0）**；PSR/DSR ∈ [0,1]；已知分布对比参考值 |
| P2-16 涨跌停封板 | 基于**前收盘价**判定：真实涨停封死不成交；普通十字星/一字线不误判为封板 |

- [ ] 新增 **"黄金 NAV 快照"回归**：对固定参考数据 + 固定策略跑回测，断言 NAV 序列哈希与基准一致（捕捉静默正确性漂移）。
- [ ] 全部用例加 `@pytest.mark.regression` 便于单独运行。

### Phase 2 — 补齐单元级缺口（按风险/LOC，P1）

- [ ] **broker/**：`LiveBrokerAdapter` 接口契约、缺凭证 `ValueError`、与 `BrokerAdapter` 抽象一致；`adapter` 基类方法默认行为。
- [ ] **strategy/execution.py（323 行，最高风险）**：订单生成、仓位缩放、再平衡触发、信号→订单映射的单元测试（用 mock PaperEngine/OMS）。
- [ ] **strategy/dsl.py / walk_forward.py**：DSL 表达式解析与求值；`train_test_split` 切分比例；fold 边界无泄漏。
- [ ] **risk/pre_trade.py / realtime.py / factory.py**：逐条 `pre_trade` 规则（MaxPosition 等）命中/未命中；`factory.build_risk_engine(load_risk_config())` 规则数=4；realtime 实时拦截路径。
- [ ] **portfolio/methods.py 属性测试**：用 `hypothesis` 验证 `Σw≈1`、风险平价风险贡献相等（容差）、kelly 有界且不爆仓。
- [ ] **analytics/stats.py**：统计量边界（空序列、单点、全相等）不抛异常且返回合理值。
- [ ] **market/rate_limiter.py / ticker.py**：限流计数与窗口；ticker 解析。
- [ ] **store/exporter.py / config/hot_reload.py / visualization/downsample.py / monitor/ws.py**：导出格式、热加载触发、降采样保形、WS 消息协议。

### Phase 3 — Dash 回调单元级测试（P1）

- [ ] 直接 import 并调用 13 个回调模块的函数（构造 `dash.development.Component`/State/Input 或内部辅助），断言输出，而非仅靠 HTTP 200。
- [ ] 重点 `backtest_callbacks.py`：断言 `_collect_bar_rows` 正确补 `trade_date` 与 `bar_time`；主回测路径确实传入 `slippage_bps=5`、`risk_engine`、`seed=42`；6 处 `bars_pl` 构造一致。
- [ ] 对 layout/pages 增加组件存在性与回调注册数的断言。

### Phase 4 — 强化集成与端到端（P1）

- [ ] **全链路 E2E**：回测 → 组合构建 → OMS → 风险 → 持仓，在固定数据集上跑通并断言中间态。
- [ ] **多市场流**：A股 + 港股通混合标的。
- [ ] **故障注入**：数据缺口、连续涨跌停、停牌、复权因子缺失，断言系统优雅处理不崩。

### Phase 5 — 性能、规模与并发（P2）

- [ ] 大规模回测基准：500 标的 × 5 年日线，断言时延 < 预算、内存不爆炸（标记 `slow`）。
- [ ] `strategy/execution.py` 时延基准。
- [ ] 并发安全：复用 `TestConnectThreadLock` 思路，对 `on_bar` 并发、`DuckDBManager` 读写并发做压力测试。

### Phase 6 — 质量门禁与 CI（P0/P1）

- [ ] 新增 `.github/workflows/test.yml`：checkout → 装依赖（含 test extra）→ `pytest`（带 `--cov`）→ 上传覆盖率报告。
- [ ] 门禁：`fail_under` 行覆盖初设 60%、逐步提到 80%；分支覆盖纳入跟踪。
- [ ] CI 内加 `ruff` 与 `mypy` 检查。
- [ ] 周期性**变异测试**（mutmut/cosmic-ray）针对 `performance.py`/`methods.py`/`fill.py`，确认测试能抓错。
- [ ] 确定性门禁：Phase 1 的种子可复现测试纳入 CI 必跑。

---

## 五、测试设计标准（Standards）

- **无真实网络**：所有 akshare/外部 IO 在 unit/integration 中必须 mock；仅 `tests/manual/` 或显式 `slow` 允许真实调用。
- **确定性**：回测/随机相关测试固定 `seed`；参考数据固定化（fixtures 或小型 CSV/JSON）。
- **属性 + 容差**：数值模块用 `hypothesis` 做属性测试，标量比较用 `pytest.approx(rel=1e-6)`。
- **命名**：`test_<module>_<behavior>_<scenario>`，场景含 normal/edge/negative。
- **隔离**：单例/全局在 fixture 中 reset（沿用 `reset_singletons` 模式）；用 `tmp_path` 不落盘到项目。
- **标记**：规模/慢用例统一 `@pytest.mark.slow`，CI 默认 `not slow` 快跑、nightly 全量。
- **断言强度**：禁止纯 `no_crash`；必须验证关键输出数值/状态。

---

## 六、实施路线图（Roadmap）

| 阶段 | 优先级 | 预计工作量 | 依赖 |
|---|---|---|---|
| Phase 0 跑起来 + 去网络依赖 | P0 | 0.5 天 | 无 |
| Phase 1 锁 16 项修复 | P0 | 1 天 | Phase 0 |
| Phase 6 CI + 覆盖率门禁 | P0/P1 | 0.5 天 | Phase 0、1 |
| Phase 2 单元缺口 | P1 | 2~3 天 | Phase 0 |
| Phase 3 Dash 回调单元 | P1 | 1.5 天 | Phase 0 |
| Phase 4 集成/E2E | P1 | 1.5 天 | Phase 2、3 |
| Phase 5 性能/并发 | P2 | 1 天 | Phase 2 |
| 变异测试 | P2 | 0.5 天 | Phase 6 |

**首个可交付里程碑**：Phase 0 + Phase 1 + Phase 6 合并为"可信基线"——`pytest` 一键绿、覆盖率报告、CI 门禁、16 项关键修复不再游离。

---

## 七、立即可执行的下一步

1. 安装测试依赖并跑出**当前真实基线**（通过/失败数 + 覆盖率）——这是制定一切阈值的前提。
2. 将根目录两个关键脚本改造进 `tests/regression/`，使其被 CI 收集。
3. mock 化 `test_market_akshare.py`，消除网络依赖。

> 上述三步可在得到确认后立即执行，预计半天内产出"可跑 + 有关键回归 + 无网络依赖"的可信测试基线。
